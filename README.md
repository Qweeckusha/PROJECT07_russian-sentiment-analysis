## Все выводы и объяснения внутри notebook -> markdown-cells
Мне было просто лень писать README.md, потому что допоздна работал с проектом :(

Я сделаю, честно


## Deploy
Для варианта на CPU - меньше скорость, но быстрее в установке зависимостей, но медленно в обработке.
> К сожалению с использование GPU в TensorFlow я так и не разобрался. 

В разделе **Embeddings NEXT-GEN (sentence-transformer)** использовалась видеокарта для обработки при помощи CUDA ядер


```bash

pip install -r requirements.txt
```
